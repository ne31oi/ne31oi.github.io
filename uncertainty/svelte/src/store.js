import { writable,readable } from 'svelte/store';

export const timeframe = writable({});

export const exchange = writable({});
export const Binance = writable({});
export const Bittrex = writable({});
export const Okex = writable({});

